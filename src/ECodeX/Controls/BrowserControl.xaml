<UserControl x:Class="ECodeX.Controls.BrowserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 浏览器工具栏 -->
        <Grid Grid.Row="0" Background="{StaticResource SurfaceTabBackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="BackButton"
                    Content="&#xE72B;" FontFamily="Segoe MDL2 Assets"
                    ToolTip="后退"
                    Style="{StaticResource ECodeXButton}" Click="Back_Click"
                    Width="32" Height="28" Padding="0" Margin="4,0,0,0" FontSize="12" />

            <Button Grid.Column="1" x:Name="ForwardButton"
                    Content="&#xE72A;" FontFamily="Segoe MDL2 Assets"
                    ToolTip="前进"
                    Style="{StaticResource ECodeXButton}" Click="Forward_Click"
                    Width="32" Height="28" Padding="0" FontSize="12" />

            <Button Grid.Column="2" x:Name="ReloadButton"
                    Content="&#xE72C;" FontFamily="Segoe MDL2 Assets"
                    ToolTip="刷新"
                    Style="{StaticResource ECodeXButton}" Click="Reload_Click"
                    Width="32" Height="28" Padding="0" FontSize="12" />

            <Button Grid.Column="3" x:Name="StopButton"
                    Content="&#xE711;" FontFamily="Segoe MDL2 Assets"
                    ToolTip="停止加载"
                    Style="{StaticResource ECodeXButton}" Click="Stop_Click"
                    Width="32" Height="28" Padding="0" FontSize="10" />

            <!-- 地址栏 -->
            <Border Grid.Column="4" CornerRadius="6" Margin="4"
                    Background="{StaticResource SurfaceHighBrush}">
                <TextBox x:Name="AddressBar"
                         Background="Transparent" Foreground="{StaticResource ForegroundBrush}"
                         CaretBrush="{StaticResource AccentBrush}"
                         BorderThickness="0" FontSize="12" Padding="8,4"
                         VerticalContentAlignment="Center"
                         KeyDown="AddressBar_KeyDown" />
            </Border>

            <Button Grid.Column="5" x:Name="DevToolsButton"
                    Content="&#xE943;" FontFamily="Segoe MDL2 Assets"
                    ToolTip="打开开发者工具"
                    Style="{StaticResource ECodeXButton}" Click="DevTools_Click"
                    Width="32" Height="28" Padding="0" FontSize="12" />

            <Button Grid.Column="6" Content="&#xE10A;" FontFamily="Segoe MDL2 Assets"
                    ToolTip="关闭浏览器"
                    Style="{StaticResource ECodeXButton}" Click="CloseBrowser_Click"
                    Width="32" Height="28" Padding="0" Margin="0,0,4,0" FontSize="10" />
        </Grid>

        <ProgressBar x:Name="LoadingProgress"
                     Grid.Row="0"
                     Height="2"
                     VerticalAlignment="Bottom"
                     IsIndeterminate="True"
                     Visibility="Collapsed"
                     Foreground="{StaticResource AccentBrush}"
                     Background="Transparent" />

        <Grid Grid.Row="1">
            <!-- WebView2 -->
            <wv2:WebView2 x:Name="WebView"
                          NavigationStarting="WebView_NavigationStarting"
                          NavigationCompleted="WebView_NavigationCompleted"
                          SourceChanged="WebView_SourceChanged" />

            <Border x:Name="ErrorOverlay"
                    Visibility="Collapsed"
                    Background="{StaticResource BackgroundBrush}"
                    Padding="32">
                <StackPanel MaxWidth="520" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="无法启动内置浏览器"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource ForegroundBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock x:Name="ErrorMessageText"
                               TextWrapping="Wrap"
                               Margin="0,12,0,16"
                               Foreground="{StaticResource ForegroundDimBrush}"
                               HorizontalAlignment="Center"
                               TextAlignment="Center" />
                    <Button Content="下载 WebView2 Runtime"
                            Style="{StaticResource ECodeXButton}"
                            HorizontalAlignment="Center"
                            Padding="14,8"
                            Click="DownloadWebView2_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
