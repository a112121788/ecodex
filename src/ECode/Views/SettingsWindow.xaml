<Window x:Class="ECode.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Height="600" Width="700"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- 在设置窗口内强制覆盖为深色系统画刷 -->
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF1A1A1A" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF1A1A1A" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FFE2E2E9" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FFE2E2E9" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF252547" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFE2E2E9" />

        <!-- 导航按钮样式 -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundDimBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="5" Margin="4,1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#20FFFFFF" />
                            </Trigger>
                            <Trigger Property="Tag" Value="active">
                                <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
                                <Setter TargetName="Bd" Property="Background" Value="#30FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 设置文本输入框样式 -->
        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FF1A1A1A" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="CaretBrush" Value="{StaticResource ForegroundBrush}" />
        </Style>

        <Style x:Key="SettingsPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#FF1A1A1A" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="CaretBrush" Value="{StaticResource ForegroundBrush}" />
        </Style>

        <Style x:Key="SettingsComboItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#FF1A1A1A" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            <Setter Property="Padding" Value="6,4" />
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#FF252547" />
                    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FF1E1E32" />
                    <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 设置下拉框样式 -->
        <Style x:Key="SettingsCombo" TargetType="ComboBox">
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF1A1A1A" />
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF1A1A1A" />
                <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FFE2E2E9" />
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FFE2E2E9" />
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF252547" />
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFE2E2E9" />
            </Style.Resources>
            <Setter Property="Background" Value="#FF1A1A1A" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource SettingsComboItem}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4" />

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="28" />
                                </Grid.ColumnDefinitions>

                                <ContentPresenter x:Name="ContentSite"
                                                  Grid.Column="0"
                                                  Margin="8,0,4,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  IsHitTestVisible="False" />

                                <TextBox x:Name="PART_EditableTextBox"
                                         Grid.Column="0"
                                         Margin="8,0,4,0"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{TemplateBinding Foreground}"
                                         VerticalContentAlignment="Center"
                                         Visibility="Hidden" />

                                <Border Grid.Column="1" Background="#FF252547" CornerRadius="0,4,4,0" IsHitTestVisible="False">
                                    <TextBlock Text="&#xE70D;" FontFamily="Segoe MDL2 Assets"
                                               Foreground="{TemplateBinding Foreground}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               FontSize="10" />
                                </Border>

                                <ToggleButton Grid.ColumnSpan="2"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </Grid>

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   StaysOpen="False"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        Margin="0,2,0,0"
                                        Background="#FF1A1A1A"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="40" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 设置复选框样式 -->
        <Style x:Key="SettingsCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <!-- 小节标签 -->
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource ForegroundDimBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="SettingsListBox" TargetType="ListBox">
            <Setter Property="Background" Value="#FF1A1A1A" />
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="Padding" Value="2" />
        </Style>

        <Style x:Key="SettingsListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="6,4" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FF252547" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#331A1A2E" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}" CornerRadius="10" BorderThickness="1"
            BorderBrush="{StaticResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.6" Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="设置" FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource ForegroundBrush}"
                               VerticalAlignment="Center" Margin="18,0,0,0" />
                    <Button x:Name="CloseBtn" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Margin="0,0,10,0" Click="CloseButton_Click"
                            Style="{StaticResource IconButton}" Content="&#xE8BB;"
                            FontFamily="Segoe MDL2 Assets" FontSize="12" />
                </Grid>
            </Border>

            <!-- 主内容区 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="1" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- 左侧导航栏 -->
                <StackPanel Grid.Column="0" Margin="0,8,0,0">
                    <Button x:Name="NavAppearance" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="外观" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavTerminal" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE756;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="终端" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavBehavior" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="行为" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavKeyboard" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE765;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="键盘" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="NavAdvanced" Style="{StaticResource NavButton}"
                            Click="NavButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="高级" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- 分隔线 -->
                <Border Grid.Column="1" Background="{StaticResource BorderBrush}" />

                <!-- 内容区域 -->
                <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Padding="24,16">
                    <Grid>

                    <!-- ═══ 外观部分 ═══ -->
                    <StackPanel x:Name="AppearanceSection">
                        <TextBlock Text="Appearance" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- 字体 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="字体" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="FontFamilyCombo" Grid.Column="1"
                                      Style="{StaticResource SettingsCombo}"
                                      Background="#FF1A1A1A" Foreground="{StaticResource ForegroundBrush}"
                                      Width="320" HorizontalAlignment="Left"
                                      IsTextSearchEnabled="True" />
                        </Grid>

                        <!-- 字号 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="字号" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Slider x:Name="FontSizeSlider" Minimum="9" Maximum="28"
                                        Width="220" VerticalAlignment="Center"
                                        TickFrequency="1" IsSnapToTickEnabled="True"
                                        ValueChanged="FontSizeSlider_ValueChanged" />
                                <TextBlock x:Name="FontSizeValueText" Foreground="{StaticResource ForegroundDimBrush}"
                                           VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12" />
                            </StackPanel>
                        </Grid>

                        <!-- 主题 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="主题" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <ComboBox x:Name="ThemeCombo" Style="{StaticResource SettingsCombo}"
                                          Background="#FF1A1A1A" Foreground="{StaticResource ForegroundBrush}"
                                          Width="200" SelectionChanged="ThemeCombo_SelectionChanged" />
                                <Border x:Name="ThemePreview" Width="60" Height="30" Margin="12,0,0,0"
                                        CornerRadius="4" BorderThickness="1"
                                        BorderBrush="{StaticResource BorderBrush}">
                                    <TextBlock x:Name="ThemePreviewText" Text="Aa" FontSize="13"
                                               FontWeight="SemiBold" HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Border>
                            </StackPanel>
                        </Grid>

                        <!-- 不透明度 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="不透明度" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Slider x:Name="OpacitySlider" Minimum="0.5" Maximum="1.0"
                                        Width="200" VerticalAlignment="Center"
                                        TickFrequency="0.01" IsSnapToTickEnabled="True"
                                        ValueChanged="OpacitySlider_ValueChanged" />
                                <TextBlock x:Name="OpacityValueText" Foreground="{StaticResource ForegroundDimBrush}"
                                           VerticalAlignment="Center" Margin="10,0,0,0" FontSize="12" />
                            </StackPanel>
                        </Grid>

                        <!-- 光标样式 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="光标样式" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="CursorStyleCombo" Grid.Column="1"
                                      Style="{StaticResource SettingsCombo}"
                                      Background="#FF1A1A1A" Foreground="{StaticResource ForegroundBrush}"
                                      Width="140" HorizontalAlignment="Left" />
                        </Grid>

                        <!-- 光标闪烁 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="光标闪烁" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="CursorBlinkCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}" />
                        </Grid>

                        <!-- 系统主题（已检测） -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="跟随系统" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="SystemThemeText" Text="深色" FontSize="13"
                                           Foreground="{StaticResource ForegroundBrush}" />
                                <TextBlock Text="（已检测）" FontSize="12" Foreground="#FF6B7280" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <!-- ═══ 终端部分 ═══ -->
                    <StackPanel x:Name="TerminalSection" Visibility="Collapsed">
                        <TextBlock Text="Terminal" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- 配色预设 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="配色预设" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="TerminalThemePresetCombo" Grid.Column="1"
                                      Style="{StaticResource SettingsCombo}"
                                      Background="#FF1A1A1A" Foreground="{StaticResource ForegroundBrush}"
                                      Width="240" HorizontalAlignment="Left"
                                      SelectionChanged="TerminalThemePresetCombo_SelectionChanged" />
                        </Grid>

                        <!-- 使用自定义颜色 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="自定义颜色" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="UseCustomTerminalColorsCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="覆盖预设颜色"
                                      Checked="UseCustomTerminalColorsCheck_Changed"
                                      Unchecked="UseCustomTerminalColorsCheck_Changed" />
                        </Grid>

                        <!-- 终端颜色色块 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="背景" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalBackgroundColorPanel">
                                <Border x:Name="TerminalBackgroundPreview" Width="30" Height="20" CornerRadius="3"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,8,0" />
                                <TextBox x:Name="TerminalBackgroundHexBox" Style="{StaticResource SettingsTextBox}" Width="110" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="选取" Style="{StaticResource ECodeButton}" Padding="10,5"
                                        Click="PickTerminalBackgroundColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="前景" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalForegroundColorPanel">
                                <Border x:Name="TerminalForegroundPreview" Width="30" Height="20" CornerRadius="3"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,8,0" />
                                <TextBox x:Name="TerminalForegroundHexBox" Style="{StaticResource SettingsTextBox}" Width="110" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="选取" Style="{StaticResource ECodeButton}" Padding="10,5"
                                        Click="PickTerminalForegroundColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="光标" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalCursorColorPanel">
                                <Border x:Name="TerminalCursorPreview" Width="30" Height="20" CornerRadius="3"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,8,0" />
                                <TextBox x:Name="TerminalCursorHexBox" Style="{StaticResource SettingsTextBox}" Width="110" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="选取" Style="{StaticResource ECodeButton}" Padding="10,5"
                                        Click="PickTerminalCursorColor_Click" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="选区" Style="{StaticResource SectionLabel}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" x:Name="TerminalSelectionColorPanel">
                                <Border x:Name="TerminalSelectionPreview" Width="30" Height="20" CornerRadius="3"
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,8,0" />
                                <TextBox x:Name="TerminalSelectionHexBox" Style="{StaticResource SettingsTextBox}" Width="110" Margin="0,0,8,0"
                                         TextChanged="TerminalColorHexBox_TextChanged" />
                                <Button Content="选取" Style="{StaticResource ECodeButton}" Padding="10,5"
                                        Click="PickTerminalSelectionColor_Click" />
                                <Button Content="重置" Style="{StaticResource ECodeButton}" Padding="10,5" Margin="8,0,0,0"
                                        Click="ResetTerminalColors_Click" />
                            </StackPanel>
                        </Grid>

                        <!-- 默认 Shell -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="默认 Shell" Style="{StaticResource SectionLabel}" />
                            <ComboBox x:Name="ShellCombo" Grid.Column="1"
                                      Style="{StaticResource SettingsCombo}"
                                      Background="#FF1A1A1A" Foreground="{StaticResource ForegroundBrush}" />
                        </Grid>

                        <!-- Shell 参数 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Shell 参数" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="ShellArgsBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" />
                        </Grid>

                        <!-- 回滚行数 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="回滚行数" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="ScrollbackBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" Width="100"
                                     HorizontalAlignment="Left" />
                        </Grid>

                        <!-- 视觉响铃 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="视觉响铃" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="VisualBellCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}" />
                        </Grid>

                        <!-- 括号粘贴模式 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="括号粘贴模式" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="BracketedPasteCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}" />
                        </Grid>
                    </StackPanel>

                    <!-- ═══ 行为部分 ═══ -->
                    <StackPanel x:Name="BehaviorSection" Visibility="Collapsed">
                        <TextBlock Text="Behavior" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <!-- 恢复会话 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="启动时恢复上一次会话" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="RestoreSessionCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="启动时恢复上一次的会话" />
                        </Grid>

                        <!-- 自动恢复可信绑定 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="自动恢复可信绑定" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="AutoResumeTrustedBindingsCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="启动后自动执行已显式信任的 resume binding" />
                        </Grid>

                        <!-- 关闭前确认 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="关闭前确认" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="ConfirmCloseCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="关闭窗口前询问" />
                        </Grid>

                        <!-- 自动复制 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="自动复制" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="AutoCopyCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="选中文本时复制到剪贴板" />
                        </Grid>

                        <!-- Ctrl+点击链接 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Ctrl+点击链接" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="CtrlClickUrlCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="使用 Ctrl+点击 打开 URL" />
                        </Grid>

                        <!-- 自动保存间隔 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="自动保存（秒）" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="AutoSaveBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" Width="80"
                                     HorizontalAlignment="Left" />
                        </Grid>

                        <!-- 日志保留 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="日志保留（天）" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="LogRetentionDaysBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" Width="80"
                                     HorizontalAlignment="Left" ToolTip="删除超过此天数的日志文件。设为 0 表示永久保留。" />
                        </Grid>

                        <!-- 关闭时保存终端记录 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="关闭时保存记录" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="CaptureOnCloseCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="在面板/Surface/项目/应用关闭时保存终端脚本" />
                        </Grid>

                        <!-- 清屏前保存终端记录 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="清屏前保存记录" Style="{StaticResource SectionLabel}" />
                            <CheckBox x:Name="CaptureOnClearCheck" Grid.Column="1"
                                      Style="{StaticResource SettingsCheck}"
                                      Content="在清屏前保存终端脚本" />
                        </Grid>

                        <!-- 终端记录保留 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="记录保留天数" Style="{StaticResource SectionLabel}" />
                            <TextBox x:Name="TranscriptRetentionDaysBox" Grid.Column="1"
                                     Style="{StaticResource SettingsTextBox}" Width="80"
                                     HorizontalAlignment="Left" ToolTip="删除超过此天数的终端记录。设为 0 表示永久保留。" />
                        </Grid>
                    </StackPanel>

                    <!-- ═══ 键盘部分 ═══ -->
                    <StackPanel x:Name="KeyboardSection" Visibility="Collapsed">
                        <TextBlock Text="键盘快捷键" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <Border Background="#FF1A1A1A" CornerRadius="6" Padding="16,12" Margin="0,0,0,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                    <RowDefinition Height="28" />
                                </Grid.RowDefinitions>

                                <!-- 项目 -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Ctrl+N" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="新建项目"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Ctrl+1-8" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="切换到项目"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Ctrl+Shift+W" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="关闭项目"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- 标签页 -->
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Ctrl+T" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="新建标签页"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Ctrl+W" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="关闭标签页"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Ctrl+Tab" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="5" Grid.Column="1" Text="下一标签页"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="6" Grid.Column="0" Text="Ctrl+Shift+Tab" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="6" Grid.Column="1" Text="上一标签页"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- 面板 -->
                                <TextBlock Grid.Row="7" Grid.Column="0" Text="Ctrl+D" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="7" Grid.Column="1" Text="向右分屏"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="8" Grid.Column="0" Text="Ctrl+Shift+D" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="8" Grid.Column="1" Text="向下分屏"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="9" Grid.Column="0" Text="Ctrl+Alt+Arrow" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="9" Grid.Column="1" Text="按方向聚焦面板"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="10" Grid.Column="0" Text="Ctrl+Shift+Z" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="10" Grid.Column="1" Text="切换缩放面板"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- 终端 -->
                                <TextBlock Grid.Row="11" Grid.Column="0" Text="Ctrl+Backspace" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="11" Grid.Column="1" Text="删除前一单词"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <!-- 视图 -->
                                <TextBlock Grid.Row="12" Grid.Column="0" Text="Ctrl+B" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="12" Grid.Column="1" Text="切换侧边栏"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="13" Grid.Column="0" Text="Ctrl+I" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="13" Grid.Column="1" Text="通知面板"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="14" Grid.Column="0" Text="Ctrl+Shift+U" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="14" Grid.Column="1" Text="跳到最新未读"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="15" Grid.Column="0" Text="Ctrl+Shift+F" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="15" Grid.Column="1" Text="在终端中搜索"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="16" Grid.Column="0" Text="Ctrl+Shift+P" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="16" Grid.Column="1" Text="命令面板"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="17" Grid.Column="0" Text="Ctrl+Shift+L" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="17" Grid.Column="1" Text="打开命令日志"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="18" Grid.Column="0" Text="Ctrl+Alt+H" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="18" Grid.Column="1" Text="打开命令历史选择器"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="19" Grid.Column="0" Text="Ctrl+Shift+V" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="19" Grid.Column="1" Text="打开会话存档"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />

                                <TextBlock Grid.Row="20" Grid.Column="0" Text="Ctrl+Shift+O" FontWeight="SemiBold"
                                           Foreground="{StaticResource AccentBrush}" FontSize="13" VerticalAlignment="Center" />
                                <TextBlock Grid.Row="20" Grid.Column="1" Text="恢复会话绑定"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </StackPanel>


                    <!-- ═══ 高级部分 ═══ -->
                    <StackPanel x:Name="AdvancedSection" Visibility="Collapsed">
                        <TextBlock Text="高级" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,20" />

                        <Border Background="#FF1A1A1A" CornerRadius="6" Padding="20,16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="自定义命令" FontSize="15" FontWeight="SemiBold"
                                           Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,6" />
                                <TextBlock Text="通过项目内 .ecode/ecode.json 定义命令，保存后可用 Ctrl+Shift+, 热重载，并在命令面板中搜索执行。"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="12"
                                           TextWrapping="Wrap" LineHeight="18" Margin="0,0,0,10" />
                                <TextBox IsReadOnly="True" Style="{StaticResource SettingsTextBox}"
                                         FontFamily="Cascadia Code" FontSize="11"
                                         TextWrapping="NoWrap" Height="112"
                                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                                         xml:space="preserve"><TextBox.Text>{
  "commands": [
    {
      "id": "dev",
      "label": "启动开发服务",
      "action": "npm run dev",
      "target": "currentTerminal",
      "confirm": false
    }
  ]
}</TextBox.Text></TextBox>
                            </StackPanel>
                        </Border>

                        <Border Background="#FF1A1A1A" CornerRadius="6" Padding="20,16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="兼容选项" FontSize="15" FontWeight="SemiBold"
                                           Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,10" />
                                <CheckBox x:Name="CompatMainPipeCheck" Style="{StaticResource SettingsCheck}"
                                          Content="监听旧主应用管道（cmux）" Margin="0,0,0,8" />
                                <CheckBox x:Name="CompatDaemonPipeCheck" Style="{StaticResource SettingsCheck}"
                                          Content="监听旧守护进程管道 / Mutex（cmux-daemon）" Margin="0,0,0,8" />
                                <CheckBox x:Name="CompatConfigFileCheck" Style="{StaticResource SettingsCheck}"
                                          Content="读取旧配置文件 .cmux/cmux.json" Margin="0,0,0,8" />
                                <CheckBox x:Name="CompatLegacyCliCheck" Style="{StaticResource SettingsCheck}"
                                          Content="接受旧 CLI 命令别名" />
                            </StackPanel>
                        </Border>

                        <Border Background="#FF1A1A1A" CornerRadius="6" Padding="20,16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="ECode" FontSize="20" FontWeight="Bold"
                                           Foreground="{StaticResource ForegroundBrush}" Margin="0,0,0,4" />
                                <TextBlock Text="v0.2.0" FontSize="13"
                                           Foreground="{StaticResource AccentBrush}" Margin="0,0,0,12" />
                                <TextBlock Text="现代化终端复用器，支持分屏、项目、命令面板和智能终端管理。"
                                           Foreground="{StaticResource ForegroundDimBrush}" FontSize="13"
                                           TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,12" />
                                <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,4,0,12" />
                                <TextBlock Foreground="{StaticResource ForegroundDimBrush}" FontSize="12">
                                    <Run Text="运行时：.NET 10" /><LineBreak />
                                    <Run Text="框架：WPF + CommunityToolkit.Mvvm" /><LineBreak />
                                    <Run Text="配置文件：%USERPROFILE%\.ecode\settings.json" />
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Grid>

            <!-- 底部操作栏 -->
            <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}"
                    Padding="18,12">
                <Grid>
                    <Button Content="恢复为默认值" Style="{StaticResource ECodeButton}"
                            Click="Reset_Click" HorizontalAlignment="Left" Padding="14,7" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="取消" Style="{StaticResource ECodeButton}"
                                Click="Cancel_Click" Padding="16,7" Margin="0,0,8,0" />
                        <Button Content="保存" Style="{StaticResource AccentButton}"
                                Click="Save_Click" Padding="20,7" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
