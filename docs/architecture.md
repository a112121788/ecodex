# Architecture

ECode is a Windows-native WPF app with a Core library, CLI, daemon, updater helpers, and tests.

Authoritative engineering design remains in `spec/`. This public page will summarize the stable user-facing architecture after M7 documentation is filled in.
